{
  "name": "clownvoiceai",
  "version": "1.0.0",
  "private": true,
  "description": "ClownVoiceAI - speaking coach, iOS shell around the existing web app",
  "scripts": {
    "cap:add": "cap add ios",
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0"
  }
}
